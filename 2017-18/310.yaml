---
name: German
type: major
code: '310'
degree: B.A.

result:
  all:
    - requirement: Electives

requirements:
  Electives:
    result:
      from: courses
      where:
        $and:
          - subject: {$eq: GERM}
          - number: {$gt: 231}

      limit:
        - at-most: 1
          where: {attributes: {$eq: german_in_translation}}

      all:
        - assert: {count(courses): {$gte: 9}}

        - where: {level: {$eq: 300}}
          assert: {count(courses): {$gte: 1}}
